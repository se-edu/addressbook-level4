package seedu.address.model.task;

/**
 * Provides utilities for sorting a list of Tasks.
 */
public class TaskSortUtil {
    public static final String CATEGORY_MONTH = "month";
    public static final String CATEGORY_DURATION = "month";
    public static final int NEGATIVE_DIGIT = -1;
    public static final int POSITIVE_DIGIT = 1;

    /**
     * Returns the apppropriate Person comparator given the sorting category

    public Comparator<> getComparator(String sortCategory) {
        Comparator<Task> comparator = null;

        switch (sortCategory) {
        case CATEGORY_MONTH:
            break;
        case CATEGORY_DURATION:
            break:
        default:
            assert (false); //invalid sortCategory should be identified in parser.
        }
        return comparator;
     }
     */
}
